import {
  assert,
  assertEquals,
  assertThrows,
  fail,
  assertFalse,
  assertNotEquals,
} from '@std/assert/mod.ts'

export { assert, assertEquals, assertThrows, fail, assertFalse, assertNotEquals }
